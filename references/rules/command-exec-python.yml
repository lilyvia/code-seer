id: python-command-exec
language: python
name: 命令执行漏洞（Python）
message: 检测到命令执行漏洞：执行用户控制的系统命令
description: |
  命令执行漏洞允许攻击者在服务器上执行任意系统命令。

  此规则检测到以下危险的命令执行方式：
  - os.system() 直接执行动态命令
  - subprocess.call() / subprocess.run() / subprocess.Popen() 使用 shell=True
  - eval() / exec() 执行动态代码

  风险：
  - 服务器被远程控制
  - 敏感数据泄露和篡改
  - 植入后门或恶意程序
  - 以当前进程权限进行横向移动
severity: error
risk_level: 严重
cwe: CWE-78
remediation: |
  避免直接执行用户输入，优先使用参数化调用和白名单校验。

  ```python
  # 不安全的代码 ❌
  os.system(user_input)
  subprocess.run(command, shell=True)
  eval(user_code)

  # 更安全的代码 ✅
  subprocess.run(['ls', '-la'], check=True, capture_output=True, text=True)

  allowed_commands = {'ls', 'pwd', 'whoami'}
  if user_command in allowed_commands:
      subprocess.run([user_command], check=True, capture_output=True, text=True)
  ```

  安全措施：
  - 禁止 shell=True
  - 对可执行命令做白名单验证
  - 不使用 eval/exec 处理外部输入
  - 通过最小权限运行服务进程
rule:
  any:
    - pattern: os.system($USER_INPUT)
    - pattern: subprocess.call($CMD, shell=True)
    - pattern: subprocess.run($CMD, shell=True)
    - pattern: subprocess.Popen($CMD, shell=True)
    - pattern: eval($USER_CODE)
    - pattern: exec($USER_CODE)
