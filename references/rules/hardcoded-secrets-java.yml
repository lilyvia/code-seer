id: java-hardcoded-secrets
language: java
name: 硬编码密钥（Java）
message: 检测到Java代码中的硬编码凭证：密钥、密码或连接串
description: |
  在Java项目中将密钥或密码直接写入字符串字面量，会导致凭证泄露和轮换困难。

  此规则检测到：
  - API密钥与访问令牌（apiKey、secretKey、token）
  - 明文密码赋值（password、passwd、pwd）
  - 数据库凭证与连接串（jdbc、username、dbPassword）
  - JWT Secret
  - 私钥标记与高熵随机字符串
severity: error
risk_severity: 高危
cwe: CWE-798
remediation: |
  避免在代码中硬编码凭证，改为配置中心或密钥管理服务：

  ```java
  // 不安全的代码 ❌
  String apiKey = "sk_live_1234567890abcdef";
  String password = "P@ssw0rd!";

  // 安全的代码 ✅
  String apiKey = System.getenv("API_KEY");
  String password = System.getenv("DB_PASSWORD");

  // 可结合配置管理与密钥托管
  // AWS Secrets Manager、Azure Key Vault、HashiCorp Vault
  ```
rule:
  any:
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)String\\s+(api[_-]?key|secret[_-]?key|access[_-]?token|jwt[_-]?secret)\\s*=\\s*['\"][A-Za-z0-9_\\-\\./+=]{12,}['\"]"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)String\\s+(password|pwd|passwd)\\s*=\\s*['\"][^'\"]{6,}['\"]"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)String\\s+(db[_-]?(user|username|password|pass|pwd)|database[_-]?url|connection[_-]?string)\\s*=\\s*['\"][^'\"]{8,}['\"]"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)jdbc:(mysql|postgresql|sqlserver|oracle):\\/\\/"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)['\"]AKIA[0-9A-Z]{16}['\"]"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)['\"](xox[baprs]-[A-Za-z0-9-]{10,}|ghp_[A-Za-z0-9]{20,}|sk_(live|test)_[A-Za-z0-9]{16,})['\"]"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----"
    - all:
        - pattern: |
            String $NAME = $VALUE;
        - regex: "(?i)(token|secret|key)\\s*=\\s*['\"][A-Za-z0-9+/=_\\-]{24,}['\"]"
